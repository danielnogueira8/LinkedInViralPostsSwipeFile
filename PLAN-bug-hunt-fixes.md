# Bug-hunt fix backlog

Source: full-app read-only bug hunt (org→user migration audit + 6 parallel subsystem hunters) + a pasted architectural-review list. Each item ships as its own PR: small, well-tested, no bundling. Check items off as PRs merge.

Convention: `[ ]` pending · `[~]` PR open, not merged · `[x]` merged. PR numbers filled in as opened.

---

## Shipped (all merged)

- [x] **media-sweep.ts unpaginated scan can purge a still-referenced asset** — PR [#1177](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1177). Reference scan now pages via `selectAllRows` past the PostgREST 1000-row cap.
- [x] **Interview-first users get incorrect 7-day voice cooldown** — PR [#1178](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1178). New `interview_updated_at` column (migration-099, user ran it in Supabase) separate from `generated_at`.
- [x] **Empty creator-style model output can become "ready"** — PR [#1179](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1179). `isUsableCreatorStyleProfile` semantic gate + one bounded retry before failing the row.
- [x] **Creator-style description mixes user content with system warnings** — PR [#1180](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1180). `creatorStyleQualityWarning(sampleCount)` derives the low-sample notice at read time instead of overwriting the user's own `description`.
- [x] **Malformed interview output saved as success, UI has no recovery** — PR [#1181](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1181). `synthesizeInterviewContext` retries once then throws instead of silently returning empty context; "Edit answers" button always reachable.
- [x] **Concurrent template creation can exceed 100-template cap** — PR [#1182](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1182). `claim_content_template_slot` RPC (migration-100, user ran it in Supabase) closes the count-then-insert TOCTOU race via a per-workspace advisory lock. Same race still open for categories/skills — separate follow-ups (see below).
- [~] **Model attribution logs requested alias, not actual response.model** — PR [#1183](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1183). `providerModelAttribution` wired into 15 call sites (9 `runCoworkAdapterAttempt` + 6 direct `completeChat`) so `usage_events` and telemetry log what OpenRouter actually served, not the requested constant. 3 sites need a follow-up (see below: weekly.ts, run.ts streaming, embedText/generateImage).
- [~] **Supabase `{error}` results silently treated as success** — PR [#1184](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1184). Investigated first: every genuinely critical write already checks `error`/throws (draft lifecycle, publishing, templates, creator-style/voice ops, `chat-turn.ts persistAssistant`). The only real gaps were two intentionally best-effort writes whose failures were previously invisible — `writeBatchChatMessage` (weekly-batch transcript inserts) and `patchRow` (creator-style generation status updates) — now `console.warn` on the discarded error instead of silently swallowing it. Scope narrower than the original title implied; no orphan-row bug found.
- [~] **Weekly rewrites not revalidated after transformation** — PR [#1185](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1185). Confirmed real: `weekly.ts`'s sameness-rewrite pass ran after the 120-3200 char gate with no recheck, unlike `run.ts`'s finalizer-backed loop. `isAcceptableRewrite`/`checkSameness` gained an optional `maxChars` bound (checked against the post-edit cleaned body); `weekly.ts` passes it + adds a final min-length recheck, falling back to the pre-rewrite body on either failure.
- [~] **Lead-magnet sanitization corrupts Markdown code blocks** — PR [#1186](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1186). Confirmed real: `sanitizeGeneratedLeadMagnetMarkdown` + `normalizeCollapsedMarkdownTables` ran their whitespace/em-dash/table regexes over the whole body with no fenced-block exclusion, even though lead magnets are explicitly prompted to produce copy-ready code/config snippets. Both now track fence state line-by-line (mirroring `to-linkedin.ts`'s `FENCE_RE` pattern) and skip normalization inside a fence — no AST rewrite needed, a targeted guard was enough.
- [x] **Below-contract lead magnets shown without warning** — PR [#1187](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1187). Confirmed real, and worse than described: `is_public` was hardcoded `true` regardless of the quality gate, so a below-contract resource was immediately publicly shareable with zero indication. Added `leadMagnetQualityWarning(metadata)` (mirrors `creatorStyleQualityWarning` — read-time-derived, no new column, no blocking write) rendered on the dashboard card + preview/share dialog. Public-page (`/lm/[slug]`) gating deliberately left as a separate product decision, not built here.
- [x] **Pattern-brief cron uncheckpointed, no cost reservation** — PR [#1188](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1188). Half the claim was already false ("one error aborts the rest" — `generatePatternBrief` already catches every per-workspace failure; added regression tests proving it). The real gap: zero wall-clock awareness of the route's 300s `maxDuration`, so a large-enough workspace count would run until Vercel hard-kills it mid-slice with no trace. Fixed with a deadline check before each concurrency slice (`deadlineHit: boolean` in the summary, testable via an explicit `startedAt` override). Cursor/rotation for a deadline-hit run's skipped workspaces intentionally NOT built — separate design question.
- [x] **"Latest scrape" lookup can cross workspaces** — PR [#1189](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1189). Confirmed real and easily reachable: `get_top_from_batch` (Cowork tool + hosted MCP tool, and transitively the weekly batch) queried `runs` for the most recent `status="ok"` run across ALL workspaces with no filter — any workspace's manual "Scrape now" would skew every OTHER workspace's recency window/displayed scrape date. Post/account data itself stayed correctly scoped throughout; only run timing metadata leaked. Added `latestRelevantScrape(workspaceId)` next to `trackedAccountIds`, scoped to `workspace_id = caller OR workspace_id IS NULL` (global cron), safely encoded via the existing `encodePostgrestValue`. `mcp-server/src/index.ts`'s copy is intentionally out of scope (local-only, single-tenant by design).
- [~] **Weekly source dedup stops at unordered 500-row scan** — PR [#1190](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1190). Both halves confirmed literally true, but the "500" cap is currently unreachable dead weight — the existing 56-day `ADAPTED_HORIZON_DAYS` filter alone bounds matching rows to ~56-70 at normal cadence, an order of magnitude under 500. The "unordered" defect is real regardless (no ordering guarantee absent `ORDER BY`, so an over-cap history would be non-deterministic, not a predictable recency horizon). Fixed with a one-line `.order("created_at", desc)` before the limit, matching this file's existing ordering convention elsewhere. Full table-normalization fix the backlog proposed is unwarranted at this scale — deferred. No new test: `architecture-boundaries.test.ts` forbids importing `weekly.ts` internals directly from tests.
- [x] **Production loading Clerk DEV credentials — FALSE ALARM (already resolved)**. Live-verified: navigated the browser to `https://tryswipein.com/sign-in`, checked loaded scripts — production loads `clerk.tryswipein.com/npm/@clerk/clerk-js@6/dist/clerk.browser.js`, not `*.clerk.accounts.dev`. Confirms the prod Clerk cutover (PR #1166/#1173, already in memory) is live and correct. No code change needed; closing as verified-stale rather than building a fix for a non-existent problem.
- [~] **trackedAccountIds unpaginated roster read feeds unbounded `.in()`** — PR [#1191](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1191). Query-shape claim confirmed, but unreachable at current scale (third finding this session with this shape): `MANUAL_ACCOUNT_LIMIT = 50` keeps every workspace far under both the 1000-row PostgREST cap and the 200-id `.in()` chunk threshold every downstream call site uses raw. Added a `console.error` guard firing only at the exact 1000-row truncation signature — turns a future silent-data-loss bug into a loud log — rather than adding `selectAllRows` pagination (real complexity, loses this function's documented `retryRead`/`cache()` interaction) for a cap that can't currently be hit.
- [~] **lead-magnets/creator-styles/content-templates GET lists unpaginated** — PR [#1192](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1192). Only lead-magnets is reachable — creator styles (cap 10) and content templates (cap 100) are unreachable at current scale, same shape as several other findings this session. Lead magnets have NO total-count cap (only a 10/month AI-generation limit; manual/URL imports unbounded), so a long-lived workspace can plausibly exceed 1000 over time. Swapped the bare `.select()` for `selectAllRows`; scoped to just this one route.
- [~] **salvageGroundedDraft can expose a failed-grounding draft as saveable** — PR [#1193](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1193). Confirmed the mechanism, but NOT a bug to remove — the salvage is a deliberate PR #1136 fix for grounded turns deterministically dead-ending with no post. The real gap: the unverified status lived only in the ephemeral chat message, not the artifact. Fixed by stamping `meta.needs_verification: true` on the salvaged artifact so it survives into `chat_artifacts.meta`. Board/editor UI surfacing of the flag deliberately deferred as a follow-up. See below (HIGH severity) for the two related lean-path items held pending a `COWORK_THIN_PATH` prod-state confirmation.
- [~] **Weekly-batch retries not idempotent** — PR [#1194](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1194). Real but narrower than described: the "fatal sibling" scenario requires a `UsagePersistenceError` specifically — every other worker failure mode is already caught inside the worker and downgraded to a per-slot `skipped`/`failed` status ("a worker never throws"). When that specific error DOES propagate, the retry machinery requeues with the SAME `batch_id`, and `batch_draft_slots` had no uniqueness constraint + `insertBatchDraft` had no idempotency key, so a re-run could double-insert a `chat_artifacts` row for a slot the first attempt already filed. Fixed with migration-101 (unique constraint on `(workspace_id, batch_id, slot_index)`) + `createBatchSlots` upserting instead of raw-inserting + each worker checking its own slot's status and skipping if already `filed`. Minimal fix, not the backlog's full "durable slot ledger" redesign.

---

## HIGH severity

- [ ] **Lean path skips factual/grounding safeguards** — INVESTIGATED, HELD (not a bug — a verified-intentional tradeoff, pending a scope decision). Confirmed real for lean `original`/`refine`/`multi` drafts (`enforceGrounding`/`enforceFactualSpecificity` both off, `reviewSourceFidelity` no-op'd via `leanFinalizerSpecialists` — `lib/agent/lean-finalizer.ts`, `draft-engine.ts:1256-1321`), but this is the documented, intentional tradeoff from the thin-path pivot (task #121) — only reachable if `COWORK_THIN_PATH` is on in prod, which isn't verifiable from this repo. **Needs the user to confirm the flag's actual prod state before deciding whether/how to build a fix.**
- [ ] **Source-based Cowork drafts bypass source fidelity in lean mode** — INVESTIGATED, HELD alongside the item above (same `COWORK_THIN_PATH` reachability question). Confirmed: a lean "model this source" turn keeps the deterministic numeric/timeline regex gate (`unsupportedSourceFactualSpecific`) but no-ops the semantic Sonnet `reviewSourceFidelity` judge — the check that catches meaning-level source drift regex can't. Same pending-decision status as the item above.
- [ ] **Workspace viral thresholds contaminate other workspaces** — INVESTIGATED, SCOPED, NOT STARTED (too large/consequential for an unattended pass — needs a deliberate multi-file PR, not a quick fix). PR #79 (`db/migration-075-workspace-post-classification.sql`) is confirmed **foundation only**, exactly as its own header comment says: it added `workspace_post_classification(workspace_id, post_id, is_viral, ...)` + backfill + a best-effort dual-write from `app/api/settings/route.ts`, but **zero read sites were migrated** — nothing anywhere does `.from("workspace_post_classification").select(...)`. The contamination bug itself is untouched: `app/api/settings/route.ts:80-93` still does two bulk `UPDATE posts SET is_viral=...` scoped only by `account_id` (not workspace) on save. Every consumer still reads the global column: `lib/agent/tools.ts:252,622`; `lib/mcp/register.ts:188,329`; `lib/batch/weekly.ts:632`; `lib/batch/pattern-brief.ts:69`; `lib/swipe-query.ts:219,314`; `lib/insights-query.ts:41,128,186`; `lib/landing-stats.ts:58`; `app/api/backfill-hooks/route.ts:73`. **Minimal remaining fix**: (1) stop the two bulk `posts` UPDATEs in the settings route; (2) flip each of the ~9 read sites to join/read `workspace_post_classification` scoped to the caller's workspace instead of `posts.is_viral`; (3) once verified, consider dropping `posts.is_viral` or leaving it vestigial. Real, non-trivial follow-up across ~9 files — do NOT attempt as a single unattended PR.
- [x] **match_post_embeddings RPC publicly executable + unbounded** — VERIFIED true (migration-088 defines it `security definer` with only a `greatest(1, match_count)` floor, never revoked from PUBLIC in migration-091 or anywhere else). Confirmed the only application caller is `lib/post-embeddings.ts:237` via `supabaseAdmin()` (service-role, server-side only). Fixed: `db/migration-102-lock-match-post-embeddings.sql` — `revoke all from public, anon, authenticated` + `grant to service_role` + hard cap `limit least(greatest(1, match_count), 50)`. [PR #1195](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1195), full suite green (3437 passed). Migration SQL pasted in PR body for the user to run manually.
- [x] **Fully-failed Apify scrape recorded as successful** — CONFIRMED: `lib/pipeline.ts`'s terminal `persist()` call after the scrape pool was unconditional (`phase:"done"`, no `error`), so per-account failures/empty-results (already caught individually inside the pool, only recorded into the cosmetic `progress` map) never flipped the run-level `status` off `"ok"`. `latestRelevantScrape()` then trusted a wasted Apify run as fresh data. Fixed the unambiguous case: `toScrape.length > 0 && postsCount === 0` → `status:"error"` with a message, in the same terminal `persist()` call. [PR #1196](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1196), new test in `evals/data/workspace-scrape-scope.test.ts`, full suite green (3438 passed). Deliberately NOT built: typed per-account outcome objects, an explicit "partial" status for mixed success/failure, or a retry/alerting policy — those need a product decision on the right taxonomy, left for a follow-up.
- [x] **Scrape pipeline can exceed worker deadline, replays paid work** — INVESTIGATED + FIXED. `lib/pipeline.ts`'s `runDailyPipeline` had no self-stop; the actual budget is `app/api/cron/jobs/route.ts`'s `maxDuration = 300` (Vercel Pro max) via the `type:"scrape"` background job. "No checkpoints" was overstated — recovery already existed: `lib/scrape-jobs.ts` (`findActiveScrapeRun`/`claim_scrape_run`, 20-min window) and `lib/background-jobs.ts`'s generic stale-job sweep reclaim a killed run within minutes, not forever. **The real confirmed gap was "replays paid work" specifically**: `decideScrapeGates`'s skip window (`GATE_HOURS=36`) is checked fresh at pipeline start, so a same-day retry minutes after a 300s kill re-scraped accounts the killed run already paid Apify for. Fixed both narrow gaps: (a) ported `lib/batch/pattern-brief.ts`'s `CRON_BUDGET_MS`/`CRON_DEADLINE_MARGIN_MS` self-stop pattern into the scrape `pool()` loop (now yields cleanly before the platform hard-kills it, logging `scrape_pipeline_deadline_hit`), and (b) added `excludeRecentlyScrapedInResume` (`lib/scrape-gating.ts`), applied only when `opts.runId` is present (a retry-capable invocation, never a fresh cron start) — excludes accounts with a `usage_events` row in the last hour. [PR #1202](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1202), new tests, full suite green. Did NOT build the full proposed durable-chunked-cursor-and-lease redesign — unnecessary given recovery already existed.
- [x] **Background-job lifecycle has competing authorities** — INVESTIGATED + FIXED (the narrow, mechanical half). Confirmed real mismatch: worker lease + provider-lock TTL is `DEFAULT_STALE_AFTER_SECS`/`DEFAULT_PROVIDER_LOCK_TTL_SECS = 15min` (`lib/background-jobs.ts:52-53`, can run longer via capacity requeues that reset their own clock without consuming `max_attempts`), but the voice read-path staleness check (`STALE_PENDING_MS = 5min`, `lib/voice-recovery.ts`) was sized for an old *synchronous* route (`maxDuration=30`) no longer in use, and the creator-style equivalent (`STYLE_STALE_GENERATING_MS = 8min`, `lib/creator-styles-cooldown.ts`) had the same root cause (sized for a since-removed inline `after()` flow) — every GET poll or retry could declare a still-legitimately-running worker "failed" well before its lease expired. PR #1197/#1198's CAS guards (shipped earlier this session) already prevent the resulting stale write from CORRUPTING data, but did NOT prevent (a) wasted spend — the falsely-failed worker keeps running to completion before losing the race, or (b) a genuine concurrent double-Apify/OpenRouter-call: a user's retry click after the false failure enqueues a SECOND background job while the first may still be legitimately executing. Fixed: raised both thresholds to 20 minutes, matching the same safety margin `lib/scrape-jobs.ts` already uses over its own 15-minute-lease analog (`SCRAPE_ACTIVE_WINDOW_MS`). [PR #1201](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1201), new regression tests asserting the threshold safely exceeds the lease, full suite green. The fuller "`background_jobs` as sole lifecycle authority" redesign (a genuine architecture decision) was NOT built — not required to close this specific gap.
- [x] **Creator-style generations have no run identity** — PARTIALLY FIXED (stale-overwrite half). CONFIRMED: `runCreatorStyleGeneration`'s completion write was an unconditional `.update().eq("id",...).eq("workspace_id",...)`, so a run `recoverStaleGeneratingStyle` already declared dead (8-min staleness) could still finish late and clobber a newer regenerate's result — PR #69's cooldown/dedupe only guards the transition INTO `generating`, not the completion write. Fixed by mirroring `lib/voice-generation.ts`'s existing `runToken`/`pending_started_at` CAS pattern: both entry points now stamp `generating_started_at` as a `runToken`, pass it through the background-job payload, and condition every completion write on it — a superseded run's write now matches zero rows and is dropped with a warning instead of overwriting. [PR #1197](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1197), new test `creator-style-run-token-guard.test.ts`, full suite green (3440 passed). NOT built (needs a product decision, separable): actual cancellation of an in-flight generation — no cancel endpoint exists; a superseded run still burns its Apify/LLM spend before losing the race, it just can no longer corrupt data.
- [x] **Voice generation and interview data can erase each other** — CONFIRMED one-directional (generation → interview only; the interview route already read-merge-writes correctly). `runVoiceGeneration`'s success write did a full-column overwrite of `voice_profiles.profile` with a freshly synthesized profile that never carries `interview_answers`/`interview_context` — if a context interview completed while a generation was still in flight, generation's later write silently erased it. This is pure field ownership (generation owns everything except the two interview-only fields), not a reconciliation/versioning question, so no `profile_version` CAS scheme was needed. Fix: re-read the row's current interview fields right before the success write and carry them forward, mirroring the interview route's own merge pattern. [PR #1198](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1198), new test `voice-generation-interview-preserve.test.ts`, full suite green. Note: read-then-write, not a DB-level atomic merge — closes the realistic race (fast interview POST vs. long-running generation job) but not a pathological sub-millisecond one.
- [x] **Long creator-style output can truncate the mandatory anti-copy guard** — CONFIRMED reachable, not just synthetic: `sanitizeCreatorStyleProfile` caps array item count (8) but never per-string length, and the LLM tool schema has no `maxLength`, so a verbose model response can genuinely produce a profile long enough to trigger this. `buildStylePromptBlock` (`lib/creator-styles.ts`) appended the guard LAST, then sliced the whole string to `STYLE_PROMPT_BLOCK_MAX` (4000, an arbitrary cap unrelated to any model's context window) — confirmed via direct execution that a long-enough profile drops the guard entirely, and the existing test only asserted the length cap, never that the guard survived (a false-negative that shipped green). Downstream mitigation is partial: `lib/agent/chat-turn.ts` wraps the injected block in a second originality instruction when used in a chat turn, but the *stored* `prompt_block` (persisted, dashboard-readable) stays guard-less, and any other consumer not routed through that wrapper inherits the bug. Fixed: build the guard first, reserve its budget, truncate only the variable content, append the guard last outside the slice. [PR #1199](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1199), tightened the existing test to assert the guard survives, full suite green.
- [x] **Paid-operation accounting incomplete, can fail open** — INVESTIGATED, SEVERITY DOWNGRADED (was HIGH, actually MEDIUM/LOW — none of the 3 sub-claims let a workspace bypass caps or get free paid ops; all are platform-internal cost-observability gaps). (a) Apify spend lacking workspace attribution — real (`logApifyUsage`, `lib/usage.ts`, has NO `workspace_id` column at all, unlike `logOpenRouterUsage`), but the claim overstates it: the global scrape genuinely has no workspace to charge (not a bypass), and no workspace-triggered Apify call exists today. (b) swallowed usage-insert errors — real for `lib/usage.ts` (console.error only, no retry/claim-hold), but the actual workspace-billed path (`logOpenRouterUsage`) ALREADY fails closed via `UsagePersistenceError` + `holdWorkspaceCostClaims` — this part of the claim is stale for the surface that matters. (c) pipeline embedding costs absent — CONFIRMED real: `lib/pipeline.ts`'s `embedAndStorePosts` call passes no `workspaceId`, so `embedText`'s cost-logging (`lib/openrouter.ts`, gated `if (opts.workspaceId)`) is skipped entirely — but again the global/platform embedding job, so observability-only. NOT fixed this session (correctly low-priority for "no input needed" scope): if ever prioritized, each sub-claim has a small mechanical fix (align `lib/usage.ts` with `logOpenRouterUsage`'s fail-closed pattern; pass a workspaceId into the pipeline's embedding call) — no transactional redesign needed.
- [x] **Credit/cost totals can be understated** — FULLY FIXED (initial pass got the row-cap sub-claim wrong; corrected on review). Two sub-claims: (1) row-cap on `lib/agent/rate-limit.ts`'s monthly-usage reads (`getMonthlyUsage`/`checkChatRateLimit`/`checkChatCostAllowance`, no `.limit()`/`.range()`, same bug class `lib/db-paginate.ts` was built for) — **initially misjudged as unreachable** (math assumed the code's $5 default and assumed all `usage_events` rows trace to budget-gated chat spend). CORRECTED after the user flagged the deployed prod budget is $10/mo via `CHAT_MONTHLY_BUDGET_USD` (already correctly env-driven everywhere, no hardcoded `5` found) — re-investigation found `logZernioUsage` (`lib/zernio.ts:390-403`) writes one `cost_usd:0` row per LinkedIn publish that is **never gated by the cost cap at all**, so a workspace with heavy publish volume can accumulate >1000 rows independent of chat/LLM economics entirely — reachable, not a defect-on-paper. FIXED: swapped all three reads to `selectAllRows` (`lib/db-paginate.ts`), preserving `checkChatRateLimit`/`checkChatCostAllowance`'s fail-closed behavior and `getMonthlyUsage`'s fail-soft degrade. [PR #1203](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1203), new pagination regression tests, full suite green. (2) `lib/costs.ts`'s provider summary genuinely omitting OpenRouter — CONFIRMED real and FIXED: `summarize()` only tracked `anthropic`/`apify` per-provider, `daily[].total` hardcoded `anthropic + apify`, **actually excluding** OpenRouter spend from each day's total. Admin-only `/api/costs` debug endpoint, no product decision. [PR #1200](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1200), full suite green.

## MEDIUM severity

_(all shipped — see Shipped section above)_

## LOW severity

- [x] **Publishing subsystem: 5 lower-severity findings** — SHIPPED (3 fixed, 2 already-fixed/mitigated, no code needed). [PR #1205](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1205), full suite green (3461 passed).
  1. Network-error-after-success retry can theoretically double-post (`zernio.ts`) — ALREADY MITIGATED by the existing `requestId` dedupe (`createLinkedInPost` passes `currentRow.id`). No change needed.
  2. DELETE of a `scheduled` draft can race the cron claim (`draft-lifecycle-supabase.ts remove`) — ALREADY FIXED by an earlier PR (task #78): `remove()` already does a pre-check (`"conflict"` if `publishing`/`published`) + a CAS-guarded DELETE scoped to `null/scheduled/failed`. No change needed.
  3. `firstComment` has no LinkedIn length re-check at publish time — FIXED. Added a re-check mirroring the existing body-length guard (`draft-publishing.ts`).
  4. `VIDEO_MIME` accepted AVI for direct post attach but the library path rejected it — FIXED. Removed AVI from the shared `VIDEO_MIME` set (`lib/post-media.ts`, the single source of truth) instead of special-casing it downstream; LinkedIn's publish API doesn't support AVI at all.
  5. Media re-persist write wasn't CAS-guarded on `schedule_status` — FIXED. Added `.eq("schedule_status", "publishing")`, matching every other terminal write in the publish loop.
- [x] **accounts.updateAccount unscoped write depends entirely on app-layer ownership check** — SHIPPED. `lib/tracked-creators-supabase.ts`'s `updateAccount` had no `.eq(manual_owner_workspace_id)` in the SQL; safe only because the one caller gated first. Fixed: added a defense-in-depth `.or()` guard scoping the update to unowned (global-baseline) rows OR rows owned by the calling workspace, via `encodePostgrestValue`. [PR #1204](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1204), merged.
- [x] **Model-attribution follow-up: `lib/batch/weekly.ts` `generateDraftBody`** — SHIPPED. Threaded `model: string | undefined` through the function's return (captured from the last successful `completeChat` response), wired the caller through `providerModelAttribution` so `weekly_batch_draft` usage rows attribute to the served model, not the `CHAT_MODEL` alias. [PR #1208](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1208), new test in `weekly-batch-status.test.ts`, full suite green (3476 passed).
- [x] **Model-attribution follow-up: `lib/agent/run.ts` `streamChat()` multi-round loop** — SHIPPED. Design call: every round in the loop requests the SAME `CHAT_MODEL` alias (no per-round model switching exists), so attribution uses the MOST RECENT round's served model, mirroring `generateDraftBody`'s pattern. Added an optional `StreamDelta.model` field (parsed from each SSE chunk's top-level `model`, the same field `completeChat` already relies on), attached ONLY to a delta that's already yield-worthy for another reason so a model-only first chunk stays a silent no-op — guarantees zero change to the stream's shape. Cost math stays on `CHAT_MODEL` (pricing table is keyed by it); only the `usage_events` attribution tag changes. [PR #1210](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1210), new test, full suite green (3461 passed) including zero regressions in the heavily-tested agent loop.
- [x] **Model-attribution follow-up: `lib/openrouter.ts` `embedText()`/`generateImage()`** — SHIPPED. Unlike `completeChat`'s chat-completions responses (reliably echo `model`), `/embeddings` and `/images` don't consistently document doing the same, so added an OPTIONAL `model` field to both raw response types, parsed opportunistically via `providerModelAttribution` — uses the served model when present, falls back to the requested alias (byte-identical to prior behavior) when absent. Zero regression risk either way. [PR #1209](https://github.com/danielnogueira8/LinkedInViralPostsSwipeFile/pull/1209), new tests in `post-embeddings.test.ts` + new `openrouter-image-attribution.test.ts`, full suite green (3463 passed).
- [ ] **Lead-magnet public-page gating decision** — deferred from PR #1187: should `/lm/[slug]` block/warn on a `quality_status: "review_suggested"` resource, or is a dashboard-only warning enough? A product call, not a blind fix.

## Meta

- [ ] **Re-run the 3 hunt agents that hit the session limit** — Cowork chat agent loop, generation pipelines/claims, frontend/UI hunters died mid-run on a session-limit API error and only returned partial fragments. Re-run for real verified findings (3 of 6 hunters succeeded cleanly: IDOR routes, shared-bookmarks/accounts, pagination/list routes — those are reflected above).

---

## Notes for whoever picks up an item

- Read the real current code before trusting a one-line summary above — some items may already be partially addressed by prior PRs (viral-threshold item explicitly flagged; others may have shifted since the hunt).
- Every PR: `tsc --noEmit` + `eslint` on touched files + full `vitest run` (must stay green, note the before/after count) + a migration through `npm run migrations:metadata`/`migrations:check` if the DB shape changes.
- Migrations are staged in the PR; the user runs them manually in Supabase (this repo's standing convention) — call that out explicitly in the PR description.
- Branch from `origin/main`, not a stacked branch — this repo's Vercel/GitHub webhook has been flaky, so keep history straight to avoid stranding commits off main.
