{
  "name": "linkedin-swipe-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server exposing the LinkedIn viral swipe file (read-only) and tracked accounts (read/write) to Claude.",
  "type": "module",
  "bin": {
    "linkedin-swipe-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "@supabase/supabase-js": "^2.105.4",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "@types/node": "^22.10.1"
  }
}
